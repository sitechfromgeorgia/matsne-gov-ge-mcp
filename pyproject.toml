[project]
name = "matsne-mcp"
version = "0.2.0"
description = "MCP server for Georgia's legal ecosystem (matsne + ecd.court.ge + supremecourt.ge)"
requires-python = ">=3.10"
dependencies = ["mcp>=1.0.0,<2.0.0"]
